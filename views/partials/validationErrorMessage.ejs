<% if (errors && errors.some(error => error.path === field)) { %>
<ul class="error-list">
  <% errors .filter(error => error.path === field) .forEach(error => { %>
  <li><%= error.msg %></li>
  <% }); %>
</ul>
<% } %>
